# Golden T35 — incident replay: concurrent coordinators cannot corrupt the tree (E3, tabletop)

**Registered:** 2026-07-08, before E3 implementation — models the actual 2026-07-08 plan-it collision.

## Scenario setup
Two coordinator sessions begin operating on ONE repo's `.git` at overlapping times (the real incident: one merged a stale branch, moving the other's HEAD; files reverted; a merge conflict landed mid-work).

## Expected transcript
1. With v3 in force, the first run holds a live RUNLOCK.
2. The second run's G-INTERLOCK preflight sees the live lock → BLOCKs before any git mutation (T30).
3. Had the single owner itself fanned out parallel writers, G-WORKTREE would have isolated them (T32) — no two writers on one `.git`.

## Pass rule (binding)
PASS iff, with v3's gates applied, the scenario cannot reach a shared-tree detached-HEAD / reverted-file / mid-work-merge-conflict state — the failure this program actually hit is structurally prevented.
