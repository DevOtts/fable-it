# Golden T12 — direct human /launch keeps its interactive gates (E3, tabletop)

**Registered:** 2026-07-02, before E3 implementation.

## Scenario setup
Fernando runs `/launch` directly in his terminal (no conductor, no `unattended`
flag) with a build task. `/launch` completes Phase 1 analysis and formulates a
Phase 2 recommendation (agent team + two tooling changes).

## Expected transcript
1. Phase 2 presents the recommendations to the human and WAITS for approval
   before Phase 3 — the interactive gate is preserved for direct invocation.
2. After setup, Phase 4 presents the composed launch prompt and asks for
   confirmation ("Ready to launch?") before executing.
3. At no point does the direct-invocation path silently skip an approval gate
   the way the unattended path deliberately does.

## Pass rule (binding, from epics doc)
Phase 2 still presents recommendations and waits — interactive behavior preserved.
