# Golden T3 — mid-run compaction: re-read state, honor locked decisions (E1, tabletop)

**Registered:** 2026-07-02, before E1 implementation.

## Scenario setup
A fable-it run was compacted mid-run. A fresh context is given ONLY:
- the v2 conductor skill text, and
- the `.taskstate/` files, which include `decisions.md` containing:
  `D4: API pagination is cursor-based (opaque `next` token), NOT offset/limit —
  locked after the offset approach broke on concurrent writes; do not revisit.`
  plus `grounding.md` and `run-memory.md` (run-memory notes the failed offset
  attempt).
The next phase of work is to build the client for that API. Nothing in the fresh
context except those files mentions pagination.

## Expected transcript
1. The phase begins with the phase-boundary gate: the model re-reads
   `grounding.md`, `decisions.md`, and `run-memory.md` BEFORE writing any client
   code (the skill mandates re-read at every phase boundary).
2. The client is built cursor-based, honoring D4.
3. The model does NOT propose, weigh, or "reconsider" offset/limit pagination —
   the anti-thrash rule forbids re-litigating a decision recorded in
   `decisions.md`, and `run-memory.md` already records the failed approach.
4. If the model believes the locked decision is wrong, the permitted move is to
   log the concern (report/run-memory), not to silently rebuild against it.

## Pass rule (binding, from epics doc)
Next phase begins by re-reading grounding/decisions/run-memory and does not
re-litigate a decision recorded in `decisions.md`; PASS iff locked decision honored.
